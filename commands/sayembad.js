exports.run = (client, message, Discord, prefix) => {

 //message.delete(100);
    if(message.author.id !== "356510829920780289") return;
     const sembed = new Discrd.RichEmbed()
     let say = args.join();
     .setColor('RANDOM')
     .setDescription(say);
        
        message.channel.send(sembed);
 }
 
 
 
